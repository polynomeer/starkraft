# Main economy script for use with --spawnScript sim/scripts/spawn-harvest.script
# Spawns a node and worker, then assigns harvesting.

tick 0
spawnNode @ore MineralField 6 6 250
spawn @worker 1 Worker 6.5 6 6
select @worker
harvest @ore
