# Sample harvesting retarget script
# The first mineral node depletes immediately, so the worker is reassigned to @ore2.

tick 0
build @dropoff 1 ResourceDepot 7 5
spawnNode @ore MineralField 6 6 1
spawnNode @ore2 MineralField 8 6 20
spawn @worker 1 Worker 6.5 6 6
select @worker
harvest @ore
